// Copy this file to config.js (same folder) and fill in your own Supabase
// project's values. Find them in your Supabase dashboard under
// Project Settings -> API.
//
//   SUPABASE_URL      -> "Project URL"
//   SUPABASE_ANON_KEY  -> "anon" "public" key   (NOT the service_role key —
//                          that one must never appear in client-side code)
//
// config.js is loaded by index.html before any other script.

window.CLASSROOM_ARENA_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-PUBLIC-KEY',
};
